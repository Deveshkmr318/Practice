public class StreamsAPIPractice {
    
    public static void main(String[] args) {
        

        // Here are **100 Stream API practice questions** ranging from easy to hard, divided into categories:  

        // ### Easy  
        // 1. Convert a list of integers to a list of their squares.  
        // 2. Filter even numbers from a list of integers.  
        // 3. Find the sum of all numbers in a list using Stream API.  
        // 4. Convert a list of strings to uppercase.  
        // 5. Count the number of strings with length greater than 5 in a list.  
        // 6. Sort a list of integers in ascending order.  
        // 7. Create a stream of integers from 1 to 10 and print them.  
        // 8. Find the first even number in a list using Stream API.  
        // 9. Create a list of unique elements from a given list.  
        // 10. Check if all elements in a list are positive using Stream API.  

        // ### Moderate  
        // 11. Group a list of strings by their length.  
        // 12. Partition a list of integers into even and odd numbers.  
        // 13. Find the second largest number in a list.  
        // 14. Flatten a list of lists using Stream API.  
        // 15. Create a map of string lengths from a list of strings.  
        // 16. Find the frequency of each word in a list of strings.  
        // 17. Concatenate all strings in a list separated by commas.  
        // 18. Convert a map into a list of key-value pairs.  
        // 19. Find the maximum number in a list using `reduce`.  
        // 20. Check if any number in a list is a multiple of 5.  

        // ### Hard  
        // 21. Implement a custom collector to group integers by their remainder when divided by 3.  
        // 22. Generate the Fibonacci sequence using `Stream.iterate`.  
        // 23. Find the longest string in a list of strings.  
        // 24. Find the top 3 maximum numbers in a list.  
        // 25. Remove duplicates from a list while maintaining insertion order.  
        // 26. Sort a list of custom objects (e.g., Employee) by multiple fields using Streams.  
        // 27. Write a Stream pipeline to compute factorial of a number.  
        // 28. Merge two lists of integers into one sorted list.  
        // 29. Group employees by department and find the highest-paid employee in each department.  
        // 30. Compute the average salary of employees grouped by department.  

        // ### Real-world Scenarios  
        // 31. Find all transactions greater than $500 from a list of transactions.  
        // 32. Convert a list of `LocalDate` objects to a list of formatted date strings.  
        // 33. Parse a CSV file into a list of objects using Stream API.  
        // 34. Find the most frequent character in a string.  
        // 35. Calculate the total revenue from a list of orders.  
        // 36. Find employees whose names start with 'A' and earn more than $50,000.  
        // 37. Group products by category and calculate the total inventory count for each category.  
        // 38. Generate all prime numbers less than 100 using Stream API.  
        // 39. Create a custom Stream operation to find the median of a list of integers.  
        // 40. Implement a word count program using Stream API.  

        // ### Bonus: Stream Concepts  
        // 41. Illustrate `peek` with examples for debugging purposes.  
        // 42. Demonstrate how `parallelStream` affects performance.  
        // 43. Convert a list to a map with key as length of the string and value as the string.  
        // 44. Write an example for infinite streams and apply a termination condition.  
        // 45. Showcase the difference between `map` and `flatMap`.  
        // 46. Write examples for `collect`, `toMap`, `toList`, and `toSet`.  
        // 47. Illustrate the usage of `Collectors.groupingBy` and `partitioningBy`.  
        // 48. Demonstrate short-circuiting terminal operations (`findFirst`, `findAny`, `anyMatch`).  
        // 49. Explain lazy evaluation in Streams using an example.  
        // 50. Convert a Stream to an array and explain when it is useful.  

        // Let me know if you'd like explanations or code examples for any specific questions!

    }
}
